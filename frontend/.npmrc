# react-day-picker@8 declares a date-fns ^2||^3 peer, but the project uses
# date-fns ^4 (API-compatible for the functions used). Allow the install to
# proceed deterministically across npm versions.
legacy-peer-deps=true
